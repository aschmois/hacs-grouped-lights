{
  "name": "grouped-lights-card",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "rollup -c && node -e \"import('node:fs').then(fs=>{fs.mkdirSync('../custom_components/grouped_lights/frontend',{recursive:true});fs.copyFileSync('dist/grouped-lights-card.js','../custom_components/grouped_lights/frontend/grouped-lights-card.js')})\"",
    "test": "vitest run"
  },
  "dependencies": { "lit": "^3.2.0" },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^16.0.0",
    "@rollup/plugin-typescript": "^12.1.0",
    "@rollup/plugin-terser": "^0.4.4",
    "rollup": "^4.24.0",
    "tslib": "^2.8.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "jsdom": "^25.0.0"
  }
}
